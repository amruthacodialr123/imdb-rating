{% extends 'base.html.twig' %}

{% block title %}Edit Director{% endblock %}

{% block body %}
       <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Director</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_director_index') }}"> 
                    View All Directors
                </a>
            </div>
            <div class="card-body">
            <img src="{{ asset('assets/images/' ~ director.image) }}" alt="{{ director.name }}" width="100">

    {{ include('director/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_director_index') }}">back to list</a>

    {{ include('director/_delete_form.html.twig') }}
    </div></div></div>

{% endblock %}
