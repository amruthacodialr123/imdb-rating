{% extends 'base.html.twig' %}

{% block title %}Edit Rating{% endblock %}

{% block body %}
   <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Rating</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_rating_index') }}"> 
                    View All Ratings
                </a>
            </div>
            <div class="card-body">
    {{ include('rating/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_rating_index') }}">back to list</a>

    {{ include('rating/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
