{% extends 'base.html.twig' %}

{% block title %}Rating{% endblock %}

{% block body %}
     <div class="container">
        <h2 class="text-center mt-5 mb-3">Show Rating</h2>
        <div class="card">
            <div class="card-header">
                    View All Rating
                </a>
            </div>
            <div class="card-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rating.id }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ rating.user }}</td>
            </tr>
            <tr>
                <th>Movie</th>
                <td>{{ rating.movie }}</td>
            </tr>
              <tr>
                <th>Rating</th>
                <td>{{ rating.rating }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ rating.comments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rating_index') }}">back to list</a>

    <a href="{{ path('app_rating_edit', {'id': rating.id}) }}">edit</a>

    {{ include('rating/_delete_form.html.twig') }}
              </div>
        </div>
    </div>
{% endblock %}
